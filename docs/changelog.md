# Changelog

## 2026-07-16

### 修正
- **設定画面のllama-serverビルド表示を修正**: `/llama/status` エンドポイントが二重登録されており、設定画面の「現在のビルド」が常に「未インストール」と表示されていた。インストール状態は `/llama/local-status` へ分離して解消。
- **チャートメニューの枠線色を修正**: 未定義のCSS変数 `--border` を参照していたため、移動平均線・価格帯別出来高メニューの枠線が意図した色になっていなかった（`--line` に修正）。
- **チャットDBの並行書き込み対策**: `chat.db` への接続に `busy_timeout` を設定し、複数処理が同時に書き込んだ際の `database is locked` エラーを予防。ベクトル検索が無効なときはログに警告を出すようにした。
- **レビュー取得時のDB接続リークを修正**: 日足・スナップショット保存が例外時に接続を閉じない問題をtry/finallyで解消。

### 追加
- **株価チャートのタイムマシン表示**: チャート下のスライダーと「◀ / ▶」ボタンで表示終端の日付を過去へ動かせるようになった。移動平均線・価格帯別出来高・山谷ラベルはその日を「今日」として再計算されるため、過去のある時点でチャートがどう見えていたかを遡って観察できる。
  - スライダーで大きく移動、◀ ▶ で1営業日ずつコマ送り、「最新へ」で一発復帰。
  - 過去表示中は見出しの日付がオレンジ色になり「（過去表示）」と表示される。株価と前日比もその日の終値・前日終値に切り替わる。
- **表示日付の強調**: 見出しの日付をバッジ表示に変更。過去表示中はオレンジの塗りつぶしになり、いつ時点のチャートかが一目で分かる。
  - 銘柄を切り替えると最新表示に戻る。
- **ウォッチリストのカテゴリー分け（タブ切り替え）**: ウォッチリストをカテゴリーごとのタブで整理できるようになった。
  - パネル右上の「＋カテゴリー」ボタン、または銘柄の追加・編集時の「＋ 新しいカテゴリーを作成...」でカテゴリーを作成。
  - カテゴリーを作ると一覧上部に「すべて / 各カテゴリー / 未分類」のタブが表示され、クリックで絞り込み。選択タブは次回起動時も維持する。株式情報・企業指標の両モードで有効。
  - 銘柄の行をタブへドラッグ＆ドロップするとそのカテゴリーへ移動できる。編集モーダルのカテゴリー欄からも変更可能。
  - アクティブなカテゴリータブの「...」メニューから名前の変更（既存名に変更した場合は統合）と削除ができる。削除したカテゴリーの銘柄は未分類に移動する（銘柄自体は消えない）。
- **株価チャートの高さをドラッグで調整可能に**: 個別銘柄レビューのチャート下端にハンドルを追加。上下ドラッグで高さを180〜640pxの範囲で変更でき、下の指標・ノート領域との比率を好みに調整できる。ダブルクリックで初期値（290px）に戻る。高さは次回起動時も維持する。

### 変更
- **株価チャートの見出しを銘柄情報に変更**: 固定の「株価チャート」テキストをやめ、選択中の企業名とコード（例: トヨタ自動車 (7203.T)）と最新データの日付（例: 2026/07/16 時点）を表示するようにした。銘柄未選択時は従来どおり「株価チャート」を表示。
- **株価チャートの操作部の見た目を統一**: 期間プルダウンだけ大きかったのを、移動平均線・価格帯別出来高・日足を再取得と同じ高さ（28px）・フォントサイズに揃えた。
- **チャット通信処理の内部共通化**: 全体チャットと銘柄チャットで別々に実装されていたAPI呼び出し・ストリーム受信処理を共通モジュールへ統合した（ユーザーから見える動作の変更はなし）。
- **価格帯別出来高の細かさを選択可能に**: チェックボックスを「価格帯別出来高」メニューに変更し、表示のオン・オフに加えて価格帯の本数（12本 / 24本 / 48本）を選べるようにした。選択は次回起動時も維持する。従来はチャートの高さから自動で決めていた（おおむね15本前後）。
- **モデル選択バーに最小幅を設定**: モデル未選択時にバーが極端に狭くならないようにした。
- **モデル一覧の表示名をファイル名だけに**: モデル選択ウィンドウの各行がフォルダ名＋ファイル名で長くなっていたのを、ファイル名のみの表示に変更した。フルパス相当（フォルダ名を含む名前）はマウスオーバーのツールチップで確認できる。

## 2026-07-15

### 追加
- **ローソク足チャートに価格帯別出来高を追加**: 表示期間の日足を価格帯ごとに集計し、チャート右側へ横棒で表示する。チェックボックスで表示を切り替えられ、選択状態は次回起動時も維持する。

### 変更
- **移動平均線の操作をメニュー化**: チャート上部のMA25・MA50・MA75チェックボックスを「移動平均線」ボタンから開くメニューへまとめ、操作部をコンパクトにした。

## 2026-07-14

### 追加
- **株価チャートに日足の再取得ボタンを追加**: 個別銘柄レビューで終値・始値・高値・安値・出来高だけをYahoo Financeから再取得できる。指標やニュースを含むレビュー全体を読み直さず、保存済みの日足を維持したまま取得できた日付を上書きする。

### 修正
- **欠損した終値が0円と表示される問題を修正**: Yahoo Financeの日足に `NaN` や欠損値が含まれる場合は保存せず、既存DBの不完全な行もチャート対象から除外する。最新日が不完全な場合は直前の正常な取引日までを表示する。

## 2026-07-13

### 追加
- **個別銘柄レビューにローソク足チャートを追加**: 始値・高値・安値・終値と出来高を表示。1か月 / 3か月 / 6か月 / 1年 / 全期間を切り替えられ、カーソル位置のOHLCVを確認できる。
- **株価履歴のローカル蓄積**: レビュー表示ごとに直近1年の日足を `app.db` の `review_price_history` へ追記。同じ日は最新値で更新し、取得範囲より古い日足は削除せず蓄積する。
- **チャートの価格ガイドを追加**: ローソク足の価格エリアにカーソルを重ねると水平線が追従し、右端にその位置の価格を表示する。
- **下部ステータスバーを追加**: 価格更新、レビュー取得、チャット生成、モデル操作、ノート更新、設定変更などの処理状況と完了・エラーを画面下部へ表示する。システムリソースは同じバーの右端に横並びで表示する。
- **チャット回答に生成情報を追加**: 回答末尾に生成速度、生成トークン数、生成時間、終了理由を表示する。エージェントがツールを複数回使った場合は、各モデル呼び出しの値を合算する。

### 変更
- **チャートの初期表示サイズを安定化**: 画面切替直後とチャート領域のサイズ変更後に実寸で再描画し、起動直後やタブを開いた直後にグラフが縦横へ引き伸ばされる問題を修正した。
- **左ナビのアイコンを変更**: ポートフォリオを円グラフ、個別銘柄レビューを株価チャート風のアイコンに変更した。
- **モデル選択UIの状態表示を改善**: 丸い状態表示をCPUアイコンに変更。モデル選択後は設定画面を自動で閉じ、ロード中のモデル名と流れるプログレスバーを上部バーに表示する。設定画面のctx表記は「コンテキスト長：32K」のような分かりやすい表記へ変更した。
- **チャートのツールチップ見切れを修正**: 右端・左端にマウスを重ねてもOHLCV情報がチャート内に収まるよう、実際の横幅に応じて表示位置を調整し、狭い場合は折り返すようにした。
- **保有銘柄・ウォッチリストの操作をメニュー化**: 行末の「編集」「×」ボタンを小さめの「...」ボタンにまとめ、ポップアップメニューから編集・削除を選ぶ形に変更した。
- **ローソク足チャートの縦横比を調整**: チャートの高さを広げ、短期間表示でローソク足が細く離れすぎないよう最大幅も少し太くした。
- **ローソク足の配色を赤・緑に変更**: 上昇日を赤、下落日を緑で表示するようにした。
- **ローソク足の縦軸を切りのよい目盛りに変更**: 価格帯に応じて `1 / 2 / 5 x 10の累乗` の間隔を自動選択し、軸の上下端も同じ間隔に揃えた。
- **山・谷の価格ラベルを追加**: 表示期間内の局所的な高値・安値を抽出し、重なりを避けて最大6点の価格をチャート上に表示。期間内の最高値・最安値は必ず含める。 山・谷の価格はどちらも明るいグレーで表示する。
- **移動平均線を追加**: MA25 / MA50 / MA75 を個別にオン・オフできるようにし、選択状態を localStorage に保存。表示期間より前の履歴も計算に使う。
- **個別銘柄レビューをキャッシュ先行表示に変更**: 前回取得した指標・財務・チャートを `app.db` から先に表示し、裏でYahoo Financeの最新データを取得して差し替えるようにした。更新中はチャートに表示し、通信失敗時も保存済みデータを残す。
- **起動時のPython依存関係確認を最適化**: `start.bat` でのpip実行を、`.venv` の新規作成時または `requirements.txt` の内容変更時だけにした。通常起動ではpipの確認ログを表示しない。
- **個別銘柄レビューの左カラムを上下分割**: 上部にローソク足チャートを固定し、下部の指標／ノート領域だけをスクロールする配置に変更。チャート上部に現在株価と前日比（金額・率）を表示する。
- **投資レビューノートに「ここ数年の経緯」を追加**: 会話からノートへ反映する際、事業構成・業績・経営方針・重要イベントの変化を時系列で整理する。
- **銘柄チャットのテンプレート質問を変更**: 「バリュエーションは割安？割高？」を削除し、「ここ数年の会社の経緯と変化を整理して」を追加。

### 開発
- **ノート生成プロンプトの設計資料を追加**: `docs/design/stock-review-note-prompt.md` に、調査用・ノート編集用のシステムプロンプト、部分反映・全面再生成のuser message、ノート構成を記載。

## 2026-07-12

### 変更
- **ノートへの反映を手動選択に変更**: 銘柄チャットの応答完了ごとに無条件でノートを自動更新する方式をやめ、各回答の下に「ノートに反映」ボタンを表示するようにした。反映したいやり取りだけを選んでノートに取り込める（過去の会話の回答にもボタンが付く）。反映中は「反映中...」、成功後は「反映済み」になる。うっかり関係ない話をしてもノートに混ざらない。
- **ノートを1世代バックアップ**: ノートの保存（反映・作り直し）前に、直前の内容を `notes.md.bak` へ退避するようにした。ノートタブの右端に「元に戻す」ボタンが出て、押すと保存前の内容に戻る（現在の内容とバックアップを入れ替える方式なので、もう一度押すと戻す前に戻れる）。
- **反映プロンプトに関連性フィルタを追加**: 銘柄の投資判断・企業分析に関係しない話題（雑談・操作の話など）はノートに書かず、反映すべき内容が無ければ既存ノートをそのまま出力するルールをマージプロンプトに追加。

### 追加
- **ノートの最終更新日時を常時表示**: 銘柄レビューのノートタブ右端に、`notes.md` の最終更新日時を表示するようにした。これまではその場で自動更新されたときだけ「HH:MM 更新」と出て、アプリを開き直すと消えていたが、ファイルの更新日時をバックエンドから返すことで銘柄を開いた時点から表示される。当日は「HH:MM 更新」、それ以前は「YYYY/M/D HH:MM 更新」形式。ノートがまだ空の銘柄では表示しない。

## 2026-07-10（第2弾）

### 変更
- **LLM モデル管理を単一サーバー構成にシンプル化**: 「常駐（standard）/ 深堀り（deep）」の役割ベース2ポート運用を廃止。起動直後は何もロードされておらず、上部バーの「モデルを設定」から `models/` フォルダの GGUF 一覧を表示 → クリックでロード → その1つのモデルがチャット・ノート更新などすべての処理を担当する。モーダルは「状態表示 + コンテキスト長選択 + 停止」と「モデル一覧（クリックでロード、稼働中表示）」に刷新。自動起動（常駐）設定は廃止。ポートは :8091 のみ使用（:8092 は解放）。旧設定からはモデル選択を自動移行し、アップグレード時に旧 standard サーバーが稼働中ならそのまま引き継ぐ。
- **ノートに出典URLを溜めない**: ノート自動更新・「ノートを作り直す」の両方に「出典URLの羅列（リンク集・出典セクション）を書かない」ルールを追加。会話を重ねるたびに出典リストが際限なく増えるのを防ぐ（重要な出典1〜2件を本文に添えるのは可。生の出典は会話ログに残る）。
- **テンプレート質問チップと入力欄の間隔を微調整**（少し空けた）。

### 修正
- **銘柄チャットが ROE 等の単位を誤読する問題を修正**: yfinance は ROE を小数（0.519 = 51.9%）で返すが、チャットのシステムプロンプトに生の値を渡していたため、LLM が「ROE 0.519%と極めて低い」のように誤解釈していた。PER/PBR/ROE/時価総額を画面表示と同じ整形済みの値（「51.9%」「76.07 倍」等）で渡すよう修正。あわせて「指標は Yahoo Finance 由来の参考値で、重要な判断では一次情報の確認を促す」注意書きをプロンプトに追加。

## 2026-07-10

### 追加
- **個別銘柄レビューに「ノート」タブを追加**: 左カラム上部に「指標 / ノート」タブを新設し、銘柄別ノート（`stocks/<ticker>/notes.md`）を Markdown 描画で表示できるようにした。ノートがまだ無い銘柄では案内メッセージを表示。
- **銘柄チャットからノートを自動更新**: 銘柄チャットで応答が完了するたびに、背景処理（standard モデル、`/chat/stream`）が「既存ノート + 新しいやり取り」を統合した更新版ノートを生成して `notes.md` に保存し、左のノート表示を自動で更新する。指標タブを開いている間に更新があった場合は「ノート」タブに黄色いドットで通知。タブ行の右端に最終更新時刻／「更新中...」／エラーを表示。既存の「会話をMarkdownにまとめる」ボタン（会話全体からの再生成）も残し、保存後にノート表示へ即時反映するようにした。

- **銘柄チャットにテンプレート質問チップを追加**: 会話が空のとき（新しい会話・会話が無い銘柄・会話削除後）に「最近のニュースを教えて」「直近の決算のポイントは？」「強みと競合優位性を整理して」「バリュエーションは割安？割高？」のチップを表示。クリックでそのまま送信され、会話がまだ無い銘柄では会話の作成から送信まで自動で行う。

### 変更
- **「会話をMarkdownにまとめる」ボタンを「ノートを作り直す」に改名**: 自動更新が基本フローになったため、手動ボタンは「会話全体からノートを全面再生成する」役割として明確化（ツールチップ付き）。
- **テンプレート質問チップを常時表示に変更**: 会話が空のときだけメッセージ領域に出す方式から、入力欄の上の固定行に常時表示する方式へ変更（会話開始後も使える）。応答の生成中は押せない。

### 修正
- **モデル設定モーダルが開くまで数秒待たされる問題を修正**: llama-server の死活確認が原因。(1) 待ち受けの無いポートへの接続が Windows 環境で拒否されずタイムアウト（2秒）まで待たされるため、HTTP プローブの前に短い TCP 接続チェック（0.3秒）を挟むようにした。(2) `get_roles_status` が役割ごとの逐次プローブ + `chat_role()` での再プローブで同じ待ちを重複していたため、全役割を並列に1回だけプローブして結果を使い回すようにした。(3) フロントの `/models` と `/llama/roles` の取得を並列化。実測で約4.0秒 → 約0.3秒。ステータスバーの10秒間隔ポーリングやチャット開始時の deep→standard フォールバック判定も同様に速くなる。

### 開発
- AUTOSHOT（`STOCK_REVIEW_AUTOSHOT=1`）がレビュー画面のノートタブに切り替えた状態（`-review-notes`）も撮影するように拡張。

## 2026-07-07

### 変更
- **常駐 LLM（standard）を既定で自動起動しないように変更**: これまではバックエンド起動時に standard モデルを毎回自動起動（常駐）していたが、既定を OFF にした。モデル設定モーダルの standard カードに「バックエンド起動時に自動起動（常駐）」チェックボックスを追加し、ON にしたときだけ従来どおり常駐する。ノートPC 等で LLM を使わない/小さいモデルに切り替える運用に対応（設定は `llama_paths.json` のマシン固有設定として保存）。standard を要約・フォールバックに使う場合も、必要時に手動起動または要求時ロードで動く。

### 追加
- **データフォルダ（保存先ルート）の設定 + 初回セットアップ**（アプリとデータの分離）:
  - **初回起動フロー**: データ保存先が未設定のときは、メイン画面の代わりに初回セットアップ画面（`setup.html`）を表示。「フォルダを選択・作成…」から既存フォルダを選ぶか新規作成するまでメイン機能に進まない（**既定フォルダへの暗黙フォールバックは廃止** — 保存先を決めるまでアプリは開始しない）。設定後は自動でメイン画面へ遷移し backend を起動。
  - **保存先の変更**: 設定 > 「データ」タブで現在の保存先表示・変更（フォルダ選択/新規作成）・フォルダを開く。変更するとそのフォルダのデータに切り替わる（自動で再読み込み）。設定した保存先が見つからない（外付けドライブ未接続・削除など）ときも初回セットアップ画面に戻る。
  - パス解決を一本化: バックエンドは `backend/paths.py`、Electron は `electron/paths.js` に集約。Electron が環境変数（`STOCK_REVIEW_DATA_DIR` / `STOCK_REVIEW_CONFIG_DIR`）で Python 子プロセスへルートを渡す。
  - 環境設定の分離: `llama_paths.json`（マシン固有のモデルパス等）はユーザーデータルートに含めず、アプリ設定領域（`app.getPath("userData")`）へ分離（初回に旧 `data/llama_paths.json` から自動移行）。保存先ルートのポインタ（`config.json`）も同領域に置き、ユーザーデータの移動先に依存しない。
  - 注意: 変更は「保存先の切り替え」であり、既存データの自動コピー/移行は行わない（フォルダは事前に用意/コピーしておく）。指定フォルダが空なら初回に既定ファイルを作成する。

### 変更
- **ポートフォリオのエクスポート／インポートを廃止**: ヘッダーの「エクスポート」「インポート」ボタンを削除。データの可搬性は上記のデータフォルダ設定（フォルダごとバックアップ/移動）が担う。ヘッダーは「価格を更新」のみになった。

### 修正
- **設定をビュー遷移から切り離し、現在の画面の上にポップアップ表示**: 設定ボタンも `.nav-button` だったため、ビュー切り替えの対象（`navButtons`）に含まれ、押すと `activateView(undefined)` が走って全ビューが非表示になり、開いていた画面（例: ポートフォリオ）のモードから外れていた。`navButtons` を `data-view` を持つナビだけに限定し、設定は別扱いに。ポートフォリオ等を開いたまま、その上に設定ポップアップが重なるようにした。
- **設定ウィンドウをポップアップとして最前面に表示**: これまで設定オーバーレイにレイヤー順（z-index）が無く、左サイドバーや上部バーが設定の上に重なって「画面に埋め込まれたモード」のように見えていた。オーバーレイを全ビュー・全クロームより前面（z-index 100）に浮かせ、背後を暗転・ブラーする本来のポップアップ表示にした（他のモーダルも同様）。

### 追加
- **配色テーマ（デザインテンプレート）機能**: 設定 > 表示に「テーマ（配色）」を新設。従来の配色を「ダーク（デフォルト）」とし、添付デザインの色調に合わせた「ネイビー（濃紺）」を追加。各テーマはミニプレビュー付きのスウォッチで選べる。選択はこの端末に保存され、次回起動時もちらつきなく適用される。
  - 土台として、ボタン・入力・スクロールバー・損益の緑/赤・ポップオーバー/モーダル背景など、これまで個別指定だった色を CSS 変数に集約。今後は `styles.css` の `:root[data-theme="xxx"]` を追加するだけで新しいテーマを増やせる。
  - 既知の残課題: キャンバス描画のチャート（資産推移・保有割合）は現状ハードコード色のままで、テーマ切り替えに完全には追従しない（別途対応予定）。

### 変更
- **全体のフォントサイズを拡大**: ルートの基準フォントを 13px → 14px に変更し、UI 全体（見出し・本文・テーブル・チャット・設定など）を約8%大きく表示。
- **個別銘柄レビューのデザインを統一**: データパネルと銘柄チャットの見た目を揃えた（同じ背景・角丸・余白、見出しはすべて同サイズ）。指標はラベル=グレー / 値=白の階層に整理し、数値は等幅表示。チャットの本文・入力欄のフォントをデータ側に合わせて縮小し、チャット内の見出しも小さめのスケールに変更。
- **チャットの Markdown テーブル対応**: `|` 区切りの表を罫線付きテーブルとして描画（従来は生テキストのまま表示されていた）。
- 現金カードを通常は表示のみに変更し、ペンアイコンを押すと入力欄に切り替わる編集モードを追加（Enter/欄外クリックで保存、Escape でキャンセル）。

### 修正
- 前回の異常終了で残った旧チャットサーバーがポート 8001 を占有していると、新しいサーバーが起動できず全リクエストが 401 になる問題を修正（起動時に 8001 の残留プロセスを掃除）。
- レイアウト検証（AUTOSHOT）で銘柄チップをクリックし、データが入った状態のレビュー画面を撮影するように拡張。

## 2026-07-05

### 変更（UI 微調整・フィードバック反映）
- リソースモニターのバーの縦幅を縮小（6px → 4px）。
- 現金（買付余力）の独立した入力欄を廃止し、サマリーの「現金」カードの金額をその場で直接編集できるように変更（点線下線が編集可能の目印。Enter か欄外クリックで保存）。
- 保有割合パネルのヘッダー（タイトル・配色・まとめる）を1行に整理し、ドーナツチャートの表示領域を拡大。チャートが縦に見切れる問題を修正（半径をキャンバス高さ基準で計算、ラベル引き出し線の余白を確保）。
- 個別銘柄レビューの財務サマリーを他のパネルと同じ幅に変更（横長を解消）。
- 銘柄チャットの「新しい会話」を大きなボタンから会話リストヘッダーの「＋」アイコンに変更。

### 変更（UI 整理: ダッシュボード化）
- **ポートフォリオをダッシュボード化**: 1920x1080 のコンテンツ領域に全体が収まるレイアウトに変更。評価額推移・保有割合・銘柄別損益の3チャートを1行に小さく並べ、保有銘柄とウォッチリストを並列配置（テーブルは内部スクロール）。1500px 未満の画面では従来どおりの縦積み＋ページスクロールに自動フォールバック。
- **個別銘柄レビューを2カラム化**: 左に指標グリッド（概要・バリュエーション・収益性・財務サマリー・アナリスト情報）、右に銘柄チャットを全高で配置。チャットの入力欄が常に画面内に収まる。
- **最新ニュースパネルを削除**（レビュービュー）。ニュースはチャットの「ニュース検索」ツールに役割を移行。
- **銘柄チャットの会話を削除できるように**: 会話一覧の各項目に削除ボタン（×、確認ダイアログ付き）を追加。
- **銘柄チャットの回答を Markdown 表示に**（全体チャットと同じレンダラーを共通化、`src/chat-markdown.js`）。
- ビュー切り替え時にチャートを再描画し、非表示中に描画されたキャンバスが潰れる問題を修正。
- 開発用: `STOCK_REVIEW_AUTOSHOT=1` で起動すると各ビューのスクリーンショットを撮って自動終了（レイアウト検証用）。

### 追加（チャット再設計・第2弾: 役割ベースモデル管理）
- **モデルを2役割で運用**: 「常駐」（:8091、軽量モデル。ノート要約などの背景処理と、深堀り停止時のチャットのフォールバック）と「深堀り」（:8092、高性能モデル。チャット優先、VRAM 節約のため手動でロード/アンロード）。チャットは深堀り優先で自動フォールバックし、使用モデル名を回答メタに表示。
- **モデル設定モーダルを役割別 UI に刷新**: 役割ごとにモデル（GGUF）とコンテキスト長を選択し、起動/停止をワンクリックで。設定は保存され、常駐モデルはアプリ起動時に自動起動。
- **銘柄ノート要約が高速化**: 要約は常駐モデル + 思考無効（`enable_thinking:false`）で実行（実測 0.5 秒。思考有効だと約1000トークンの思考が先行する）。
- 旧・単一サーバー構成（:8080）からは自動移行（選択中モデルは「深堀り」に引き継ぎ）。
- ポート割り当てを news-picker と衝突しないよう調整（news-picker が 8081/8082 を使用中のため 8091/8092 を採用）。

### 追加（チャット再設計・第1弾）
- **エージェンティックチャット**: チャット（全体・銘柄別とも）が Web 検索・ニュース検索・銘柄指標取得をツールとして自律的に使い、出典 URL 付きで回答するようになった（`POST /chat/agent-stream`）。最大8ターンのツール使用、失敗時はモデルに伝えて続行、上限到達時は手持ち情報で回答を強制。
- **ツール活動と思考の表示**: 回答中に「🔍 ニュース検索: …→ N件」の活動ログと、折りたたみ式の「思考」（reasoning モデルの思考過程）を表示。
- **Web 検索**: `ddgs`（DuckDuckGo、API キー不要）による一般検索とニュース検索（`backend/search_web.py`）。検索失敗・0件は静かに空を返し、会話は止めない。
- **Ornith-1.0 対応**: llama-server を `--jinja` で起動し、reasoning（`reasoning_content`）とツールコールのテンプレート解析を有効化。サンプリング既定 {temp 0.6, top_p 0.95, top_k 20}、チャットは max_tokens 4096/ターン。Ornith 9B/35B の GGUF を `models/` に配置済みで、モデル選択モーダルからロード可能。

### 修正
- 会話メモリ・文書の全文検索（FTS5）に `ORDER BY rank` が無く、LLM に渡すコンテキストの選定が実質ランダムだった問題を修正。
- `GET /documents/search` が `/documents/{id}` に飲まれて常に 422 を返していた問題を修正（ルート登録順）。
- 銘柄別チャットで最終 SSE イベントを取りこぼすことがある問題を修正（末尾バッファのフラッシュ）。
- 未使用の `backend/fetch_prices.py` を削除。

### 追加
- **F12 スクリーンショット**: F12 キーでコンテンツ領域のスクリーンショットを `data/screenshots/YYYYMMDD-HHmmss.png` に保存。
- ウィンドウのコンテンツ領域を 1920x1080 基準に変更（`useContentSize`。スクリーンショットも同サイズになる）。
- `portfolio.json` などのデータファイルを保存時に1世代バックアップ（`.bak`）。破損時は自動的にバックアップから復旧。

### 修正
- **同一銘柄の複数ロットがローカル DB 上で1件に潰れる問題を修正**。`holdings` テーブルをロット単位のスキーマ（自動採番の主キー）へ移行し、DB 由来の読み込み・資産推移・配当集計がロットを正しく合算するように修正（既存 DB は起動時に自動移行）。
- **ロンドン市場など補助通貨単位（GBp=ペンス等）の銘柄が 100 倍で円換算される問題を修正**。GBp/GBX/ZAc/ILA を主要通貨に正規化してから換算。
- 評価額推移チャートが履歴不足時に架空のダミー系列（サイン波）を表示していた問題を修正。履歴が無い場合は「履歴がまだありません」と明示し、前日比・期間変動も表示しない。
- 通貨情報が取得できない場合のデフォルトが経路によって JPY / USD と不一致だった問題を修正。推測での換算をやめ、明示的にエラーとして銘柄ごとに表示。
- 過去の為替レートが欠損している日付に当日のスポットレートを使っていた問題を修正（直近過去のレートで補完）。
- 配当利回りが 1% 以下のとき 100 倍で表示されることがある問題を修正（桁推測ヒューリスティックを廃止）。
- 現金残高などの数値入力で全角数字が 0 円として保存される問題を修正（NFKC 正規化）。
- チャットの一部メッセージが文字化けして表示される問題を修正。
- 銘柄ノート（notes.md）や設定ファイルの書き込みをアトミック化し、クラッシュ時のファイル破損を防止。
- 保存処理の並行実行で「database is locked」により DB 同期が静かに失敗し得た問題を緩和（WAL + busy_timeout）。

### セキュリティ
- チャットサーバー（127.0.0.1:8001）に起動ごとに生成するトークン認証を追加し、CORS をアプリ（file:// 起点）のみに制限。ブラウザ上の任意サイトからチャット履歴・ノートの読み取りや削除・pip 実行系エンドポイントを叩けた問題を修正。
- ニュースタイトル・セクター名など外部（Yahoo/JPX）由来の文字列を HTML エスケープして描画（XSS 対策）。ニュースリンクは http(s) のみ許可。
- 新規ウィンドウ・外部ページへの遷移を禁止し、外部リンクは既定ブラウザで開くように変更（`setWindowOpenHandler` / `will-navigate` ガード）。
- Electron を 35（EOL）から 43 へ更新。

### 変更
- 買値入力欄に「円で入力」の注記を追加（外貨建て銘柄の損益は円建てで計算されるため。通貨対応は今後の課題）。

## 2026-06-29

### 変更
- 評価額推移チャートの「期間」（1ヶ月〜1年）と「軸」（相対軸 / 絶対軸）のプルダウン選択を localStorage に保存し、アプリ再起動後も維持するように変更。

### 修正
- 設定した現金残高（買付余力）がアプリ再起動後に 0 に戻ってしまう不具合を修正。現金は `portfolio.json` に `cash` キーで保存される一方、読み込み側が `cashJpy` キーだけを見ていたためで、両方のキーを受け付けるよう修正。
- 同一銘柄を複数ロット保有している環境で、ローカル DB への初回移行（レガシー JSON 取り込み）が重複ティッカーで失敗する不具合を修正（upsert 化）。

## 2026-06-28

### 追加
- ポートフォリオに現金残高（買付余力）を設定できるように追加。サマリーに「現金」と、株式評価額と合算した「保有資産評価（株式＋現金）」のカードを併記（従来の「総評価額」は「株式評価額」に名称変更）。
- 保有割合チャートに現金スライスを追加し、現金比率（キャッシュポジション）を可視化。
- 現金残高はローカル DB（`app_settings` テーブル）に保存し、再起動後も保持。

## 2026-06-26

### 追加
- 保有テーブル / 保有割合チャートの「同一銘柄をまとめる」と、保有割合チャートの配色モード（デフォルト / セクター別）を再起動後も保持するように変更。

## 2026-06-25

### 追加
- 設定ウィンドウを新設（左ナビ最下部の歯車アイコンから開く、タブ構成）。
  - **llama-cpp**: 最新リリースを確認し、Windows ビルド（CPU / CUDA / Vulkan ほか）をダウンロード・展開。CUDA は対応する cudart も同梱。
  - **埋め込み**: 埋め込みモデル（ruri-v3-310m）の状態表示と手動ダウンロード。依存（sentence-transformers ほか）が未導入なら、アプリ内から `.venv` へ自動インストール（PyTorch 込み）→ 続けてモデル取得。
  - **表示**: 「リソースモニターを表示」チェックボックス。ON で上部バーに CPU / RAM / GPU / VRAM の使用量をバー表示（依存 psutil / nvidia-ml-py は未導入なら自動インストール）。
- 保有割合チャートに配色プルダウンを追加（デフォルト配色 / セクター別グラデーション）。

### 変更
- 「保有銘柄数」を、重複保有を除いたユニーク銘柄数でカウントするように変更。
- llama-server のバイナリ保存先を `bin/llama-server/` から `runtime/llama-server/` へ移行（旧 `bin/` も後方互換で参照）。

### 修正
- 保有割合チャートで銘柄名ラベルが画面端で見切れる問題を修正。
- 保有割合チャートが広い画面で小さく見える問題を修正。キャンバス幅とドーナツ半径を表示領域に合わせて拡大するように変更。
- チャット画面で会話入力欄が見切れる問題を修正。会話（メッセージ）と会話ツリーだけが内部スクロールし、入力欄は常に表示。
- チャット画面の後続CSSで高さ指定が上書きされ、入力欄が再び見切れる問題を修正。

### その他
- `data/` 配下の個人データを git 管理対象外に変更（テンプレートと参照データは維持）。
- 開発計画ドキュメント（`docs/plan/`）と `CLAUDE.md` を整備。
- `CLAUDE.md` を参考に、Codex / エージェント向けの `AGENTS.md` を追加。
