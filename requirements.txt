# Data
yfinance>=1.0.0
numpy>=1.24.0
pandas>=2.0.0

# UI
gradio>=6.0.0

# Config
pyyaml>=6.0

# LLM — llama-cpp-python (GGUF, Windows + CUDA 対応)
# CUDA 対応版インストール:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
# CPU のみの場合:
#   pip install llama-cpp-python
llama-cpp-python>=0.3.0

# API backend (replaces Gradio)
fastapi>=0.111.0
uvicorn>=0.29.0

# Testing
pytest>=7.0.0
pytest-mock>=3.10.0
